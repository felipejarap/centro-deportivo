eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_Auth?useSSL=false&serverTimezone=UTC
    username: root
    password:
  jpa:
    show-sql: true
jwt:
  secret: clave-super-secreta-debe-tener-256-bits-minimo!!